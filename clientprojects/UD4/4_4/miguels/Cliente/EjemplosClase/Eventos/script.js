'use strict';

const btnTelcado = document.getElementById("input-teclado");
const logEventos = document.getElementById("log-eventos");

btnTelcado.addEventListener("click", (event) => {
    if (event) {
    }
})
